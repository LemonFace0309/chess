#ifndef _PIECE_ENUM_
#define _PIECE_ENUM_

// k = king, q = queen, b = bishop, n = knight, r = rook, p = pawn.
//   Lowercase letters are black pieces, and uppercase is white. 
enum PieceEnum {
  k,
  q,
  b,
  r,
  n,
  p,
  K,
  Q,
  B,
  R,
  N,
  P,
  NONE
};
#endif